<% if current_user  %>
<% if @cart_items.size == 0  %>
  <h2>
    There are no items in your shopping cart.  Please <%= link_to "go back", products_path %> and add some items to your cart.
  </h2>
  <% else %>
  <h1>
  <% @cart_items.each do |order_item| %>
    <%= order_item.product.name %><br>
    <%= order_item.product.price %><br>
    <%= order_item.quantity %><br> 
    <%= order_item.pirce %><br>
    
     


<% end %>
</h1>
 <% end %>
  <% end %>